name: dsl_flutter_example
description: Example usage of dsl_flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  analyzer: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  flutter_lints: ^6.0.0

  dsl_flutter:
    path: ../

flutter:
  uses-material-design: true